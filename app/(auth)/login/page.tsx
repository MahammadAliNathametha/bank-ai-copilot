import { LoginWorkspace } from "@/components/auth/login-workspace";

export const metadata = {
  title: "Login | Bank AI Copilot"
};

export default function LoginPage() {
  return <LoginWorkspace />;
}
